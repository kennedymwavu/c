#include <stdio.h>

int main() {
  char letter = 'A';
  printf("fav letter is '%c', and in ascii it is '%d'\n", letter, letter);

  char name[] = "mwavu";
  printf("hello, %s!\n", name);
  return 0;
}
